[project]
name = "ghost"
version = "0"
description = "Panel tooling for the GHOST NOTES VCV Rack plugin"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["panelkit", "pytest>=8", "pytest-env"]

[tool.uv.sources]
panelkit = { path = "/Users/davidpalaitis/Development/panelkit", editable = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
env = ["PYTHONHASHSEED=0"]
