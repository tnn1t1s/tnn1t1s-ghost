# src/embedded/

Baked-in PCM sample data as C++ headers (`Ghost*Data.hpp`) for the ROM voices
(clap, rim, crash, ride, hats). Captured from the author's own recordings and
compiled directly into the plugin, so no external sample files are needed.
