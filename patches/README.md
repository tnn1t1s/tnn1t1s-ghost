# patches/

Example `.vcv` patches for the Ghost kit.

- `demos/` — one self-contained groove per classic-909 style, plus standalone
  example patches (full kit, accent groove, bus-crush, output EP, supersonic,
  factory MIDI)
- `lessons/` — progressive build-ups, one voice at a time

Behavior-probe / regression patches live in `tests/patches/`.
